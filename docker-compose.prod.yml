# Docker Compose pour la production - Version simplifiée AMD64

services:
  # Frontend Next.js - Serveur unique - Architecture AMD64
  frontend:
    image: athenadecisionsystems/delphes-frontend:20251031-013614
    container_name: delphes-frontend-prod
    env_file:
      - .env.production
    ports:
      - "3006:3000"  # Exposition directe du port Next.js
    environment:
      - NEXT_TELEMETRY_DISABLED=1
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped